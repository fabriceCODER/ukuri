const SearchBar = () => {
    return (
        <div className="relative">
            <input type="text" placeholder="Search..." className="border p-2 rounded-md w-full" />
        </div>
    );
};

export default SearchBar;
